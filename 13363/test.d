alias IRQualifiedName = int;
int tok;

IRQualifiedName parseQualifiedName()
{
    IRQualifiedName current;

    do
    {

        if((tok))
            AfterProc:
    } while((tok));
    return current;
}
